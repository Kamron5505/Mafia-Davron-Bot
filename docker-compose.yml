version: "3.9"

services:
  bot:
    build: .
    container_name: mafia-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/mafia_bot.db
